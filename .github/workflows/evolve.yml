name: Daily Evolution

on:
  schedule:
    - cron: '0 * * * *'  # Every hour (more reliable than 15 mins)
  
  workflow_dispatch:  # Manual trigger for testing

jobs:
  evolve:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      issues: write
      pull-requests: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Configure Git
        run: |
          git config user.name "Quine Gardener"
          git config user.email "quine@garden.bot"
      
      - name: Display Current Generation
        run: |
          echo "🧬 Current Quine State:"
          echo "===================="
          head -5 quine.py
          echo ""
      
      - name: Log Current Generation
        run: |
          python3 logger.py
      
      - name: Evolve Quine
        id: evolve_step
        run: |
          echo "🔄 Running quine to produce next generation..."
          
          # 1. Create a unique branch for this mutation
          BRANCH_NAME="evolution/gen-$(grep "Generation" quine.py | head -n 1 | grep -o '[0-9]\+' || echo "next")-$(date +%s)"
          git checkout -b "$BRANCH_NAME"
          
          # 2. Run the quine and capture output
          python3 quine.py > quine_next.py
          
          # 3. Replace old with new
          mv quine_next.py quine.py
          
          # 4. Run logger to generate snapshots and log entries
          python3 logger.py
          
          echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT
          echo "✅ Evolution complete"
      
      - name: Verify Quine Integrity
        id: verify_step
        run: |
          echo "🧪 Testing self-replication..."
          python3 quine.py > test_output.py
          
          if grep -q "template =" test_output.py; then
            echo "✅ Quine integrity verified"
            rm test_output.py
          else
            echo "❌ Quine broken!"
            exit 1
          fi
      
      - name: Propose Mutation (PR)
        if: success()
        id: pr_step
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          GENERATION=$(grep "Generation" quine.py | head -n 1 | grep -o '[0-9]\+' || echo "unknown")
          DRIFT=$(grep "# Drift:" quine.py | head -n 1 || echo "No drift recorded")
          BRANCH_NAME="${{ steps.evolve_step.outputs.branch_name }}"
          
          git add -A
          git commit -m "🧬 Evolution: Generation ${GENERATION}"
          git push origin "$BRANCH_NAME"
          
          # Attempt to create PR
          echo "Attempting to create PR..."
          if PR_URL=$(gh pr create --title "🧬 Mutation Proposal: Generation ${GENERATION}" \
            --body "## Genetic Drift Detected
            
            - **Generation:** ${GENERATION}
            - **Observations:** ${DRIFT}
            
            *The garden is proposing this change to its own genome.*" \
            --base main --head "$BRANCH_NAME" 2>&1); then
            echo "pr_url=$PR_URL" >> $GITHUB_OUTPUT
            echo "✅ PR created: $PR_URL"
          else
            echo "⚠️ PR creation failed: $PR_URL"
            echo "This is likely a permission issue. Falling back to direct commit if possible..."
            echo "pr_failed=true" >> $GITHUB_OUTPUT
          fi
      
      - name: Auto-Merge Mutation
        if: success() && steps.pr_step.outputs.pr_failed != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_URL="${{ steps.pr_step.outputs.pr_url }}"
          gh pr merge "$PR_URL" --merge --delete-branch
      
      - name: Fallback Direct Commit
        if: success() && steps.pr_step.outputs.pr_failed == 'true'
        run: |
          echo "Infrastructure (PR) failed, but quine is healthy. Merging directly to main..."
          git checkout main
          git pull origin main
          git merge "${{ steps.evolve_step.outputs.branch_name }}"
          git push origin main
      
      - name: Report Genetic Collapse (Issue)
        if: failure() && steps.verify_step.outcome == 'failure'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          GENERATION=$(grep "Generation" quine.py | head -n 1 | grep -o '[0-9]\+' || echo "unknown")
          
          gh issue create --title "🚨 GENETIC COLLAPSE: Generation ${GENERATION}" \
            --body "## Critical Error
            
            The latest mutation resulted in a non-replicating program. 
            The system has blocked the merge to protect the lineage.
            
            **Generation attempted:** ${GENERATION}
            **Status:** Reverted to last stable state.
            
            *Human intervention may be required if the garden stops growing.*"
      
      - name: Update README Status
        if: always()
        run: |
          # Ensure we have the latest quine info even if merge failed
          GENERATION=$(grep "Generation" quine.py | head -n 1 | grep -o '[0-9]\+' || echo "unknown")
          LINEAGE=$(grep "Lineage:" quine.py | head -n 1 | cut -d':' -f2 | xargs || echo "unknown")
          NOTE=$(grep "Nature's Note:" quine.py | head -n 1 | cut -d':' -f2 | xargs || echo "The garden is deep in thought.")
          
          git checkout main || git checkout -b main
          git pull origin main
          
          sed -i "s/> \*\*Generation:\*\*.*/> **Generation:** $GENERATION/" README.md
          sed -i "s/> \*\*Lineage:\*\*.*/> **Lineage:** $LINEAGE/" README.md
          sed -i "s/> \*\*Nature's Note:\*\*.*/> **Nature's Note:** $NOTE/" README.md
          
          git add README.md
          if [ -n "$(git status --porcelain README.md)" ]; then
            git commit -m "📝 Docs: Update Garden Status (Generation $GENERATION)"
            git push origin main
          fi

      - name: Update Statistics
        if: always()
        run: |
          python3 visualize.py
          python3 lineage.py
